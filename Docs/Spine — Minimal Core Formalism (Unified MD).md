# Spine — Minimal Core Formalism (v2.1 Unified MD with Explicit RG Flows)

**Version**: v2.1  
**Date**: Updated with explicit Wilsonian beta functions and ensemble potential flow derivations (internal consistency only).  
**Core Principle**: Single constrained continuum field. All physics emerges from its nonlinear dynamics, topology, kernels, RG flow, and stochastic floor. No spacetime primitives, no point particles, no operator fields.

---

## 0. Ontology

The theory contains a single constrained continuum field:
$n(\mathbf{x},t) \in S^2, \quad |n|^2 = 1$

All observable structures emerge as nonlinear excitations, kernel responses, stochastic transport effects, and collective resonant modes of this field.

There are:
* No spacetime primitives
* No metric geometry
* No point particles
* No operator-valued quantum fields

Matter, radiation, gravity-like behaviour, and quantum-like statistics are emergent properties of constrained continuum dynamics.

---

## 1. Fundamental Action

The dynamics are generated by:
$S[n] = \int d^4x \left[ \frac{Z_t}{2}(1+\chi \mathcal{E}[n])(\partial_t n)^2 - \mathcal{E}[n] \right]$

with energy density:
$\mathcal{E}[n] = \frac{A_1}{2}|\partial_i n|^2 + \frac{A_2}{4}|\partial_i n \times \partial_j n|^2 + \frac{A_3}{2}|\nabla^2 n|^2 + \frac{A_4}{2}(1-(n\cdot n_{\text{vac}})^2)$


| Parameter | Role |
| :--- | :--- |
| $A_1$ | gradient stiffness |
| $A_2$ | topological Skyrme interaction |
| $A_3$ | UV bi-harmonic regulator |
| $A_4$ | vacuum mass-gap constraint |
| $\chi$ | nonlinear refractive coupling |

Constraint projection:
$P_\perp(V) = V - (n \cdot V)n$

---

## 2. Equation of Motion

With constraint $n \cdot \delta n = 0$:
$\partial_t^2 n = \frac{1}{Z_t} P_\perp \Big( A_1 \nabla^2 n + A_2 \partial_i[(\partial_j n \times \partial_i n)\times \partial_j n] - A_3 \nabla^4 n + A_4 (n\cdot n_{\text{vac}})n_{\text{vac}} \Big) - |\partial_t n|^2 n$

Operator closure set:
$\{k^2,\; k^4,\; k^2\omega^2\}$

---

## 3. Solitons and Topology

Topological charge:
$Q = \frac{1}{4\pi} \int d^3x\, \epsilon_{ijk}\, \partial_i n \cdot (\partial_j n \times \partial_k n)$


| Structure | Topology |
| :--- | :--- |
| baryons | $Q=1$ |
| leptons | $Q=0$ kernel-bound modes |
| nuclei | $Q \ge 2$ composites |

Soliton mass:
$m = \int d^3x\, \mathcal{E}_{\text{soliton}}$

---

## 4. Kernel Response Sector

Nonlocal interactions:
$\Phi(\mathbf{x}) = \int d^3x'\, K(\mathbf{x}-\mathbf{x}')\, n(\mathbf{x}')$

Energy:
$E_{\text{int}} = \frac12 \int d^3x\, n(\mathbf{x})\Phi(\mathbf{x})$

Kernel structure:
$K(k)=\frac{A}{k^2+\Lambda^2}+\frac{B}{k^2+m^2}$

Real space:
$K(r)=\frac{1}{4\pi \Delta r}(e^{-mr}-e^{-\Lambda r})$

---

### 5.1 Explicit Beta Functions (Derived)

**For $A_1$** (gradient stiffness):
$\beta_{A_1}=-A_1+cA_2\sigma_\alpha^2+\mathcal{O}(\chi\text{ mixing})$

**For $A_2$** (Skyrme):
$\beta_{A_2}=+2A_2+\gamma'_\sigma\sigma_\alpha^2A_2+\mathcal{O}(\text{higher})$

**For $A_3$** (biharmonic):
$\beta_{A_3}=-2A_3+\gamma_\sigma\sigma_\alpha^2+\mathcal{O}(\chi,A_2\text{ mixing})$

**For $\chi$** (refractive coupling):
$\beta_\chi=-\gamma_\chi\chi\sigma_\alpha^2+\eta_\chi\frac{\Pi^\ast}{A_1}\sigma_\alpha^2+\mathcal{O}(\chi^2\sigma_\alpha^4)$

**Kernel**:
$\frac{dK}{d\ell}=\beta_K$

**Response**: $\frac{d\xi\sb{R}}{d\ell}=\xi\sb{R}\left[\gamma\sb{K}\left(\frac{\partial\ln k}{\partial\ln K}\right){k\to0}-\gamma\sigma\sigma\sb{\alpha}^2\right]$




---



### 5.2 RG-Locked Invariant

Complementary flows produce:
$\Pi = g_2 A_3 \quad (\text{or } A_2 A_3)$

with
$\frac{d\Pi}{d\ell} \approx \mathcal{O}(\sigma_\alpha^2) \to \Pi^*$

This protects UV behavior via cancellation of classical scaling dimensions (+2 vs -2) within operator closure.

---

## 6. Emergent Gravity-like Sector

Linearisation:
$n = n_{\text{sol}(r)} + \psi$

Equation:
$Z_t(1+\chi E_0)\partial_t^2\psi - A_1\nabla^2\psi = 0$

Effective propagation:
$c_{\text{eff}}(r)=\frac{c_0}{\sqrt{1+\chi E_0(r)}}$

Optical metric (refractive):
$n_{\text{opt}}(r)=\sqrt{1+\chi E_0(r)}$

Observables:
$\alpha(b)=\int \partial_b \delta n\,dz$
$\Delta t=\frac{1}{c_0}\int (n_{\text{opt}}-1)\,dz$

**RG Flow of Sector**: The composite $f=\chi E_0$ satisfies $\frac{d\ln f}{d\ell}\approx-\gamma_\chi\sigma_\alpha^2+\text{small }\Pi^\ast\text{ residual}$. Refractive contrast weakens in the IR (screening by stochastic floor) while $\Pi^\ast$ protects residual soliton-anchored effects.


---

## 7. Ensemble Gravity

Emergent potential (ensemble average over solitons via kernel):
$\Phi_{\text{ens}}(r)\approx -V_{\text{flat}}^2 \ln(r/R_0)$

with
$V_{\text{flat}}^2 \propto \chi \langle E_0 \rangle \rho_{\rm sol} \Pi^*$

Lensing:
$\alpha \approx \pi V_{\text{flat}}^2$

**RG Flow of Ensemble Potential**:
$\beta_{V^2} \approx (-3 + \gamma_{\rm ens} \sigma_\alpha^2) V_{\rm flat}^2$
Classical dilution (-3) competes with fluctuation clustering. Potential flattens in IR while retaining logarithmic form protected by $\Pi^*$. Lensing strength decreases at large scales.

---

## 8. Surface-State Atomic Sector

Linearised operator:
$\hat{\mathcal{L}}_{Q=1}\psi = P_\perp\left(A_1\nabla^2\psi - A_3\nabla^4\psi + V_{\text{eff}}(r)\psi\right)$

Constraint:
$n_{\text{sol}}\cdot \psi = 0$

### 8.1 Kernel-Generated Effective Potential

Definition:
$V_{\text{eff}}(r)=\int d^3r'\,K(r-r')n_{\text{sol}}(r')$

With soliton profile:
$n_{\text{sol}}(r)\sim e^{-r/R_s}$

Result:
$V_{\text{eff}}(r)= \frac{1}{4\pi \Delta} \left[ \frac{e^{-mr}}{r(1+m^2R_s^2)} - \frac{e^{-\Lambda r}}{r(1+\Lambda^2R_s^2)} \right]$

### 8.2 Spectral Structure

Eigenvalues:
$\lambda_{n,\ell} = -\frac{A_1}{\gamma^2(n+\ell+1)^2} -\frac{A_3}{\gamma^4(n+\ell+1)^4}$

Curvature scale:
$\gamma^{-2} \sim V_{\text{eff}}''(r_0)$

---

## 9. Stochastic Quantum Floor

Noise sector:
$\hbar_{\text{eff}}=\sigma_\alpha^2\rho_0$

Transport:
$\beta' + \Gamma_\beta \beta = S_\delta\delta + \Xi_\beta$

Noise:
$\langle \Xi_\beta \Xi_\beta \rangle = N_\beta \delta(k-k')\delta(a-a')$

---

## 10. Acoustic Cosmology

Pre-projection spectrum:
$P(k)=\left[\frac{1-e^{-\Gamma(k)\tau_{\text{freeze}}}\cos(\omega(k)\tau_{\text{freeze}})}{H(k)}\right]^2$

Damping:
$\Gamma(k)=\left(\frac{\sigma_\alpha^2 A_2^2}{\Pi^*}\right)k^3$

Hessian:
$H(k)=A_1k^2+A_3k^4+A_4$

Projection:
$C_\ell \sim \int dk\,P(k)\,j_\ell^2(kD_A)$

---

## 11. Spectral Reduction & Diagnostic Layer (NON-FUNDAMENTAL)

This layer is explicitly **not part of the action or RG flow**.

### 11.1 Projection Functional
$\mathcal{P}[n] \rightarrow \{P(k), C_\ell\}$

### 11.2 Kernel of Observables


| Quantity | Status |
| :--- | :--- |
| SVD spectrum | projection basis diagnostic |
| peak structure | extremal mapping of $P(k)$ |
| transport flow | basis deformation measure |
| mode alignment | kernel eigenbasis rotation |

### 11.3 Key Principle
All numerical diagnostics are:
> eigenstructure of the projection operator, not modifications of the field theory.

---

## 12. Emergent Quantum Behaviour


| Phenomenon | Origin |
| :--- | :--- |
| interference | field overlap |
| tunnelling | boundary texture transition |
| decoherence | stochastic bath |
| spin | orientation texture |

---

## 13. Structural Nonlocality

Constraint:
$|n|^2=1$

Correlation:
$P_{\text{corr}}=\cos^2(\theta_A-\theta_B)$

---

## 14. Minimal Interpretation

Spine reduces physics to:
* constrained continuum field
* nonlinear solitons
* kernel-mediated interactions
* RG flow structure (with explicit betas and $\Pi^*$ locking)
* stochastic bath dynamics
* spectral projection observables

No spacetime, particles, or operator Hilbert structure is fundamental.

---

**End of Document**

This v2.1 incorporates explicit internally-derived beta functions for $A_1,A_2,A_3,\chi$ and RG flows for emergent/ensemble gravity sectors while preserving all original content.
