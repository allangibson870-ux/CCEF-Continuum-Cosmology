# Spine — Minimal Core Formalism (Unified MD)

## 0. Ontology
The theory contains a single constrained continuum field:
$$n(\mathbf{x},t) \in S^2, \quad \lvert n \rvert^2 = 1$$

All observable structures emerge as nonlinear excitations, kernels, and collective modes of this field. There are:
* No spacetime primitives
* No metric geometry
* No point particles
* No operator-valued quantum fields

Matter, radiation, gravity-like effects, and stochastic phenomena are entirely emergent continuum structures.

---

## 1. Fundamental Action
The continuum dynamics are generated by the action:
$$S[n] = \int d^4x \left[ \frac{Z_t}{2} (1 + \chi \mathcal{E}[n]) (\partial_t n)^2 - \mathcal{E}[n] \right]$$

with the structural energy density functional:
$$\mathcal{E}[n] = \frac{A_1}{2} \lvert \partial_i n \rvert^2 + \frac{A_2}{4} \lvert \partial_i n \times \partial_j n \rvert^2 + \frac{A_3}{2} \lvert \nabla^2 n \rvert^2 + \frac{A_4}{2} \left(1 - (n \cdot n_{\rm vac})^2\right)$$

The orthogonal projection operator enforcing the target space constraint is:
$$P_\perp(V) = V - (n \cdot V) n$$

---

## 2. Continuum Equation of Motion
Variation under the rigid constraint $n \cdot \delta n = 0$ yields the full non-linear, projected Euler-Lagrange equation:
$$\boxed{\partial_t^2 n = \frac{1}{Z_t} P_\perp \Bigl( A_1 \nabla^2 n + A_2 \partial_i [(\partial_j n \times \partial_i n) \times \partial_j n] - A_3 \nabla^4 n + A_4 (n \cdot n_{\rm vac}) n_{\rm vac} \Bigr) - \lvert \partial_t n \rvert^2 n}$$

The operator algebra $\{k^2, k^4, k^2\omega\}$ is fully closed under renormalisation.

---

## 3. Solitons and Topology
Localized stable excitations are soliton attractors of the continuum. The topological winding number is given by:
$$Q = \frac{1}{4\pi} \int d^3x \, \epsilon_{ijk} \, \partial_i n \cdot (\partial_j n \times \partial_k n)$$

### Topological Classification

| Structure | Topology |
| :--- | :--- |
| **Baryons** | $Q = 1$ |
| **Leptons** | $Q = 0$ kernel-locked modes |
| **Nuclei** | Composite $Q \geq 2$ manifolds |

Soliton mass arises strictly from the integrated localized field energy:
$$m = \int d^3x \, \mathcal{E}_{\rm soliton}$$

---

## 4. Kernel Response Sector
Nonlocal interactions arise from continuum response kernels:
$$\Phi(\mathbf{x}) = \int d^3x' \, K(\mathbf{x}-\mathbf{x}', a) n(\mathbf{x}')$$

The resulting interaction energy is:
$$E_{\rm int} = \frac{1}{2} \int d^3x \, n(\mathbf{x}) \Phi(\mathbf{x})$$

The momentum-space Green's function factorizes into a dual-pole propagator:
$$K(k) = \frac{A}{k^2 + \Lambda^2} + \frac{B}{k^2 + m^2}$$

Integrating this over 3D spatial slices yields the exact real-space operator potential:
$$\boxed{K(r) = \frac{1}{4\pi \Delta \, r} \left( e^{-m r} - e^{-\Lambda r} \right)}$$

where the physical regimes are cleanly split by mass scale inversion:
* $m \approx 10^3$ represents the heavy ultraviolet (UV) regularizing structural scale.
* $\Lambda \approx 1.88$ represents the light macroscopic infrared (IR) screening scale.

---

## 5. RG Flow Structure
Under Wilsonian coarse-graining with scale factor $b = e^\ell$, the state vector is defined as $X(\ell) = [K, \xi_R, A, B, S_i]^T$. The kernel and correlation-length flow equations progress via:
$$\frac{d\ell}{dK} = \beta_K(K, \xi_R)$$

$$\frac{d\ell}{d\xi_R} = \xi_R \left[ \gamma_K \left( \frac{\partial \ln k}{\partial \ln K} \right)_{k \to 0} - \gamma_\sigma \sigma_\alpha^2 \right]$$

Topological invariance restricts charging flow to zero: $\frac{d\ell}{dQ_i} = 0$. The physical RG manifold closes effectively onto the pair $(A_2, A_4)$, while $A_3$ acts as a redundant UV regulator.

---

## 6. Emergent Gravity-like Sector
Linearising the action around a static soliton background ($n(\mathbf{x},t) = n_{\rm sol}(r) + \psi(\mathbf{x},t)$) yields:
$$Z_t (1 + \chi E_0(r)) \partial_t^2 \psi - A_1 \nabla^2 \psi = 0$$

This gives a direction-dependent phase velocity and an effective refractive index:
$$c_{\rm eff}(r) = \frac{c_0}{\sqrt{1 + \chi E_0(r)}} \implies n_{\rm opt}(r) = \sqrt{1 + \chi E_0(r)}$$

Gravitational lensing emerges as continuum refraction rather than spacetime curvature.

### Relational Observables
* **Deflection angle:** $\alpha(b) = \int_{-\infty}^{\infty} \frac{\partial_b \delta n}{\sqrt{b^2 + z^2}} \, dz$
* **Shapiro delay:** $\Delta t = \frac{1}{c_0} \int (n_{\rm opt} - 1) \, dz$

---

## 7. Ensemble Gravity and Galactic Scaling
At large macroscopic scales, multi-soliton ensembles generate an emergent logarithmic field potential:
$$\Phi_{\rm ens}(r) \approx -V_{\rm flat}^2 \ln \left( \frac{r}{R_0} \right)$$

This layout naturally produces flat rotation curves, constant lens deflection bands, and logarithmic Shapiro delays. Large-scale lensing simplifies to:
$$\alpha \approx \pi V_{\rm flat}^2$$

This matches galactic observations directly without requiring dark matter primitives.

---

## 8. Surface-State Atomic Sector
Electrons are not point particles but standing surface modes bound to a $Q=1$ core. The linearised surface equation takes the Sturm-Liouville form:

$$\hat{\mathcal{L}}_{Q=1} \psi = \lambda \psi, \quad \text{where } \hat{\mathcal{L}}_{Q=1} \psi = A_1 \nabla^2 \psi - A_3 \nabla^4 \psi + V_{\rm eff} \psi$$


Hydrogenic reduction yields discrete energy eigenvalues:
$$\lambda_{n,\ell} = -\frac{A_1}{(n + \ell + 1)^2 \gamma^2}$$

Atomic quantization therefore emerges from continuum boundary restrictions, not Hilbert-space axioms.

---

## 9. Stochastic Quantum Floor
Irreducible fluctuations emerge internally from the variance sector $\sigma_\alpha^2(a)$, setting the effective action-density scale:
$$\hbar_{\rm eff} = \sigma_\alpha^2 \rho_0$$

The transport equation and white-noise correlator evolve according to:
$$\beta' + \Gamma_\beta \beta = S_\delta \delta + \Xi_\beta$$
$$\langle \Xi_\beta(k,a) \Xi_\beta(k',a') \rangle = N_\beta \delta^D(a-a') \delta^D(k-k')$$

This internal stochastic bath generates decoherence, spectral broadening, wavepacket diffusion, and tunneling analogues without external quantum postulates.

---

## 10. Emergent Quantum Behaviour
Quantum-like phenomena arise from the stochastic soliton ensemble dynamics:


| Quantum Phenomenon | Continuum Origin |
| :--- | :--- |
| **Wavepacket spreading** | Stochastic soliton diffusion |
| **Interference** | Coherent field overlap |
| **Tunneling** | Texture-boundary reconfiguration |
| **Lamb-shift analogue** | Stochastic shell breathing |
| **Decoherence** | Bath-induced phase scrambling |
| **Bell correlations** | Global manifold constraint |
| **Spin** | Field orientation texture |

---

## 11. Structural Nonlocality
The field remains globally tied to its target space constraint ($\lvert n(\mathbf{x},t) \rvert^2 = 1$). Measurement alignments induce global manifold re-normalisation rather than a localized wave-function collapse. The coincidence correlation map is purely geometric:
$$P_{\rm coincidence} \propto \lvert n_{\theta_A} \cdot n_{\theta_B} \rvert^2 = \cos^2(\theta_A - \theta_B)$$

Bell-type correlations arise naturally from the continuous constrained manifold geometry.

---

## 12. Minimal Physical Interpretation
CCEF reduces physics to a single constrained continuum field, nonlinear topological solitons, nonlocal kernel responses, RG-driven collective states, and internally generated stochastic fluctuations. Under this ontology:
* **Matter** = Stable topological attractors ($Q=1$ cores)
* **Gravity** = Refractive continuum response ($\chi E_0$)
* **Quantum mechanics** = Stochastic soliton ensemble dynamics
* **Cosmological scaling** = RG evolution of interacting continuum modes

No independent spacetime metric, quantum operator algebra, or particle ontology is fundamental.
