# Spine — Minimal Core Formalism (Unified MD)

## 0. Ontology

The theory contains a single constrained continuum field:
$$n(\mathbf{x},t) \in S^2, \quad |n|^2 = 1$$

All observable structures emerge as nonlinear excitations, kernels, stochastic transport sectors, and collective resonant modes of this field. There are:

* No spacetime primitives
* No metric geometry
* No point particles
* No operator-valued quantum fields

Matter, radiation, gravity-like effects, quantum-like behaviour, and cosmological observables emerge entirely from constrained continuum dynamics.

---

## 1. Fundamental Action

The continuum dynamics are generated by the action:
$$S[n] = \int d^4x \left[ \frac{Z_t}{2}(1+\chi \mathcal{E}[n])(\partial_t n)^2 - \mathcal{E}[n] \right]$$

with structural energy density:
$$\mathcal{E}[n] = \frac{A_1}{2}|\partial_i n|^2 + \frac{A_2}{4}|\partial_i n \times \partial_j n|^2 + \frac{A_3}{2}|\nabla^2 n|^2 + \frac{A_4}{2}\left(1-(n\cdot n_{\rm vac})^2\right)$$

| Parameter | Physical Role                      |
| :-------- | :--------------------------------- |
| $A_1$     | Gradient tension stiffness         |
| $A_2$     | Topological Skyrme self-repulsion  |
| $A_3$     | Bi-harmonic UV regulator           |
| $A_4$     | Vacuum mass-gap locking            |
| $\chi$    | Energy-density refractive coupling |

The orthogonal target-space projection operator enforcing the rigid constraint is:
$$P_\perp(V) = V - (n \cdot V)n$$

---

## 2. Continuum Equation of Motion

Variation under the rigid constraint:
$$n \cdot \delta n = 0$$

yields the projected nonlinear Euler-Lagrange equation:
$$\partial_t^2 n = \frac{1}{Z_t} P_\perp \Big( A_1 \nabla^2 n + A_2 \partial_i[(\partial_j n \times \partial_i n)\times \partial_j n] - A_3 \nabla^4 n + A_4(n\cdot n_{\rm vac})n_{\rm vac} \Big) - |\partial_t n|^2 n$$

The principal-symbol operator algebra:
$${k^2, k^4, k^2\omega}$$

remains closed under renormalisation flow.

---

## 3. Solitons and Topology

Localized stable excitations emerge as nonlinear topological soliton attractors of the continuum.

The winding number is:
$$Q = \frac{1}{4\pi} \int d^3x, \epsilon_{ijk}, \partial_i n \cdot (\partial_j n \times \partial_k n)$$

### Topological Classification

| Structure   | Topology                       |
| :---------- | :----------------------------- |
| **Baryons** | $Q = 1$                        |
| **Leptons** | $Q = 0$ kernel-bound modes     |
| **Nuclei**  | Composite $Q \geq 2$ manifolds |

Soliton mass arises entirely from localized field energy:
$$m = \int d^3x, \mathcal{E}_{\rm soliton}$$

---

## 4. Kernel Response Sector

Nonlocal interactions arise through continuum response kernels:
$$\Phi(\mathbf{x}) = \int d^3x', K(\mathbf{x}-\mathbf{x}',a), n(\mathbf{x}')$$

with interaction energy:
$$E_{\rm int} = \frac12 \int d^3x, n(\mathbf{x})\Phi(\mathbf{x})$$

The momentum-space Green's function factorizes into a dual-pole propagator:
$$K(k) = \frac{A}{k^2+\Lambda^2} + \frac{B}{k^2+m^2}$$

Integrating over spatial slices yields the exact real-space response kernel:
$$K(r) = \frac{1}{4\pi \Delta r}\left(e^{-mr} - e^{-\Lambda r}\right)$$

| Scale               | Physical Role                      |
| :------------------ | :--------------------------------- |
| $m \sim 10^3$       | UV structural regularization scale |
| $\Lambda \sim 1.88$ | IR macroscopic screening scale     |

---

## 5. RG Flow Structure

Under Wilsonian coarse-graining with scale factor:
$$b = e^\ell$$

the continuum state vector evolves as:
$$X(\ell) = [K, \xi_R, A, B, S_i]^T$$

with RG transport equations:
$$\frac{dK}{d\ell} = \beta_K(K,\xi_R)$$

$$\frac{d\xi_R}{d\ell} = \xi_R \left[ \gamma_K \left( \frac{\partial \ln k}{\partial \ln K} \right)*{k \to 0} - \gamma*\sigma \sigma_\alpha^2 \right]$$

Topological charge remains invariant:
$$\frac{dQ_i}{d\ell} = 0$$

The physical manifold closes effectively onto the pair:
$$(A_2, A_4)$$

while the bi-harmonic regulator $A_3$ acts as a redundant UV operator direction.

### RG-Locked Structural Invariant

The UV regulator and stochastic scattering sector obey complementary flow equations:
$$\frac{dA_3}{d\ell} = -2A_3 + \mathcal{O}(\sigma_\alpha^2 A_3)$$

$$\frac{dg_2}{d\ell} = +2g_2 - \mathcal{O}(\sigma_\alpha^2 g_2)$$

Defining the composite structural coordinate:
$$\Pi \equiv g_2 A_3$$

yields:
$$\frac{d\Pi}{d\ell} = A_3 \frac{dg_2}{d\ell} + g_2 \frac{dA_3}{d\ell}$$

which reduces to:
$$\frac{d(g_2 A_3)}{d\ell} = 0 + \mathcal{O}(\sigma_\alpha^2)$$

Thus the product:
$$\Pi^* = g_2 A_3$$

locks onto a scale-invariant fixed structural constant on the stable RG manifold.

This invariant removes the pathological UV amplification previously generated by isolated $A_3^{-1}$ scaling.

---

## 6. Emergent Gravity-like Sector

Linearising around a static soliton background:
$$n(\mathbf{x},t) = n_{\rm sol}(r) + \psi(\mathbf{x},t)$$

yields:
$$Z_t(1+\chi E_0(r))\partial_t^2\psi - A_1\nabla^2\psi = 0$$

This produces an effective refractive propagation velocity:
$$c_{\rm eff}(r) = \frac{c_0}{\sqrt{1+\chi E_0(r)}}$$

and corresponding refractive index:
$$n_{\rm opt}(r) = \sqrt{1+\chi E_0(r)}$$

Gravity-like behaviour emerges as continuum refraction rather than spacetime curvature.

### Relational Observables

#### Deflection angle

$$\alpha(b) = \int_{-\infty}^{\infty} \frac{\partial_b \delta n}{\sqrt{b^2+z^2}},dz$$

#### Shapiro delay

$$\Delta t = \frac1{c_0}\int (n_{\rm opt}-1),dz$$

---

## 7. Ensemble Gravity and Galactic Scaling

Large multi-soliton ensembles generate an emergent logarithmic potential:
$$\Phi_{\rm ens}(r) \approx -V_{\rm flat}^2 \ln\left(\frac{r}{R_0}\right)$$

This naturally produces:

* Flat galactic rotation curves
* Constant lensing bands
* Logarithmic Shapiro delays

The asymptotic lensing relation simplifies to:
$$\alpha \approx \pi V_{\rm flat}^2$$

without dark matter primitives.

---

## 8. Surface-State Atomic Sector

Electrons emerge as constrained surface modes bound to a $Q=1$ topological soliton background. Linearisation is performed on the tangent bundle of the $S^2$ manifold, so fluctuations must remain orthogonal to the background field.

The perturbation ansatz is:

$$n(\mathbf{x},t) = n_{\rm sol}(r) + \psi(\mathbf{x},t), \qquad n_{\rm sol} \cdot \psi = 0$$

To preserve the constraint, the linearised operator must remain projected:

$$\hat{\mathcal{L}}_{Q=1}\psi = P_\perp \left( A_1 \nabla^2 \psi - A_3 \nabla^4 \psi + V_{\rm eff}[n_{\rm sol}] \, \psi \right)$$

where the effective potential is no longer a free object but is generated by the soliton + kernel background:

$$V_{\rm eff}[n_{\rm sol}] = A_4 \left( 1 - (n_{\rm sol} \cdot n_{\rm vac})^2 \right) + \Phi_K[n_{\rm sol}]$$

with kernel self-energy:

$$\Phi_K n_{\text{sol}} = \int d^3x' \, K(\mathbf{x}-\mathbf{x}') \left( n_{\text{sol}}(\mathbf{x}') \cdot n_{\text{sol}}(\mathbf{x}) \right)$$


---

### Spectral Structure

In the IR-reduced radial sector, eigenmodes satisfy:

$$\lambda \psi = A_1 \nabla^2 \psi - A_3 \nabla^4 \psi + V_{\rm eff}(r) \psi$$

leading to discrete bound-state spectra:

$$\lambda_{n,\ell} \sim -\frac{A_1}{(n+\ell+1)^2\gamma^2} + \delta\lambda(A_3)$$

where:
* $\delta\lambda(A_3)$ encodes UV curvature corrections from the bi-harmonic regulator
* $A_3$ now explicitly contributes as a spectral deformation term (not just a passive regulator)

---

### Validity Regime

This linearisation is valid only in the tangent-space fluctuation regime:

* $|\psi| \ll |n_{\rm sol}|$ (small perturbation limit)
* Weak backreaction on soliton core
* Clear separation between IR bound states and UV regulator scale
* Projection constraint maintained: $n_{\rm sol} \cdot \psi = 0$



---

## 9. Stochastic Quantum Floor

Irreducible fluctuations emerge internally from the stochastic variance sector:
$$\sigma_\alpha^2(a)$$

which defines the effective action-density scale:
$$\hbar_{\rm eff} = \sigma_\alpha^2 \rho_0$$

The transport equation evolves according to:
$$\beta' + \Gamma_\beta\beta = S_\delta\delta + \Xi_\beta$$

with white-noise correlator:
$$\langle \Xi_\beta(k,a) \Xi_\beta(k',a') \rangle = N_\beta \delta^D(a-a') \delta^D(k-k')$$

This stochastic bath generates:

* Decoherence
* Spectral broadening
* Wavepacket diffusion
* Tunneling analogues
* Resonant shell fluctuations

without external quantum postulates.

---

## 10. Emergent Acoustic Cosmology

The cosmological acoustic sector emerges from resonant continuum oscillations projected through large-scale response geometry.

The pre-projection spatial power spectrum is:
$$P(k) = \left[ \frac{1 - e^{-\Gamma(k)\tau_{\rm freeze}}\cos(\omega(k)\tau_{\rm freeze})}{H(k)} \right]^2$$

where the resonant dispersion relation is:
$$\omega(k) = c_s k \left( \frac{\xi_R}{R_{\rm core}} \right)$$

and the renormalised damping law is:
$$\Gamma(k) = \left( \frac{\sigma_\alpha^2 A_2^2}{\Pi^*} \right) k^3$$

Using:
$$\sigma_\alpha^2 = 0.05, \quad A_2 = 0.3268, \quad \Pi^* \approx 1$$

gives:
$$\Gamma(k) \approx 0.00534,k^3$$

The spatial Hessian propagator is:
$$H(k) = A_1 k^2 + A_3 k^4 + A_4$$

The acoustic sector therefore exhibits:

* Genuine pre-projection oscillatory extrema
* Weakly damped phase coherence
* Surviving higher-order resonant bands
* Silk-like high-$k$ suppression
* Projection-stable angular peak structure

The angular observables emerge from line-of-sight projection:
$$C_\ell \sim \int dk, P(k), j_\ell^2(kD_A), e^{-k^2/k_D^2}$$

with:
$$D_A = c_s \tau_{\rm freeze} \approx \xi_R$$

implying:
$$M_{\rm proj} = \frac{D_A}{\xi_R} \approx 1$$

Thus the acoustic peaks emerge from genuine continuum phase coherence rather than geometric projection artifacts.

---

## 11. Emergent Quantum Behaviour

| Quantum Phenomenon       | Continuum Origin                 |
| :----------------------- | :------------------------------- |
| **Wavepacket spreading** | Stochastic soliton diffusion     |
| **Interference**         | Coherent field overlap           |
| **Tunneling**            | Texture-boundary reconfiguration |
| **Lamb-shift analogue**  | Stochastic shell breathing       |
| **Decoherence**          | Bath-induced phase scrambling    |
| **Bell correlations**    | Global manifold constraint       |
| **Spin**                 | Field orientation texture        |

---

## 12. Structural Nonlocality

The field remains globally constrained by:
$$|n(\mathbf{x},t)|^2 = 1$$

Measurement alignment induces manifold-wide renormalisation rather than localized wavefunction collapse.

The coincidence relation is:
$$P_{\rm coincidence} \propto |n_{\theta_A}\cdot n_{\theta_B}|^2 = \cos^2(\theta_A-\theta_B)$$

Bell-type correlations emerge naturally from global constrained continuum geometry.

---

## 13. Minimal Physical Interpretation

Spine reduces physics to:

* A single constrained continuum field
* Nonlinear topological solitons
* RG-driven collective transport
* Nonlocal kernel response
* Internally generated stochastic fluctuations
* Emergent resonant cosmological modes

Under this ontology:

| Observable                 | Continuum Origin                          |
| :------------------------- | :---------------------------------------- |
| **Matter**                 | Stable topological attractors             |
| **Gravity**                | Refractive continuum response             |
| **Quantum mechanics**      | Stochastic ensemble dynamics              |
| **Cosmological structure** | RG-driven continuum resonances            |
| **CMB acoustic peaks**     | Surviving coherent continuum oscillations |

No independent spacetime metric, particle ontology, or operator-valued quantum substrate is fundamental.
