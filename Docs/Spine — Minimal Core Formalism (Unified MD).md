# Spine — Minimal Core Formalism (v2.2 Unified MD with Critique Responses)

**Version**: v2.2  
**Date**: Updated with responses to kinematics, RG closure, emergent gravity, and stochastic floor critiques. All derivations remain internal to the constrained continuum field.  
**Core Principle**: Single constrained continuum field. All physics emerges from its nonlinear dynamics, topology, kernels, RG flow, and stochastic floor. No spacetime primitives, no point particles, no operator fields.

---

## 0. Ontology

The theory contains a single constrained continuum field:

$$n(\mathbf{x},t) \in S^2, \quad |n|^2 = 1$$

All observable structures emerge as nonlinear excitations, kernel responses, stochastic transport effects, and collective resonant modes of this field.

There are:
* No spacetime primitives
* No metric geometry
* No point particles
* No operator-valued quantum fields

Matter, radiation, gravity-like behaviour, and quantum-like statistics are emergent properties of constrained continuum dynamics.

---

## 1. Fundamental Action

The dynamics are generated by:

$$S[n] = \int d^4x \left[ \frac{Z_{t}}{2}(1+\chi \mathcal{E}[n])(\partial_{t} n)^2 - \mathcal{E}[n] \right]$$

with energy density:

$$\mathcal{E}[n] = \frac{A_{1}}{2}|\partial_{i} n|^2 + \frac{A_{2}}{4}|\partial_{i} n \times \partial_{j} n|^2 + \frac{A_{3}}{2}|\nabla^2 n|^2 + \frac{A_{4}}{2}(1-(n\cdot n_{\text{vac}})^2)$$


| Parameter | Role |
| :--- | :--- |
| $A_{1}$ | gradient stiffness |
| $A_{2}$ | topological Skyrme interaction |
| $A_{3}$ | UV bi-harmonic regulator |
| $A_{4}$ | vacuum mass-gap constraint |
| $\chi$ | nonlinear refractive coupling |

**Note on Kinematics (v2.2)**: The kinetic term is nonlinear and higher-derivative via $\mathcal{E}$. Full variation produces mixed spatio-temporal terms. The EOM in Section 2 uses the transverse projector $P_{\perp}$ and assumes consistent boundary conditions that suppress Ostrogradsky ghosts by keeping dynamics on the $S^2$ manifold. Explicit counter-terms or projection extensions may be needed for full initial-value well-posedness.

Constraint projection:

$$P_{\perp}(V) = V - (n \cdot V)n$$

---

## 2. Equation of Motion

With constraint $n \cdot \delta n = 0$:

$$\partial_{t}^2 n = \frac{1}{Z_{t}} P_{\perp} \Big( A_{1} \nabla^2 n + A_{2} \partial_{i}[(\partial_{j} n \times \partial_{i} n)\times \partial_{j} n] - A_{3} \nabla^4 n + A_{4} (n\cdot n_{\text{vac}})n_{\text{vac}} \Big) - |\partial_{t} n|^2 n$$

**v2.2 Clarification**: This is the leading projected form. Full Euler-Lagrange variation of the $\chi \mathcal{E} (\partial_{t} n)^2$ term generates mixed derivatives such as $\partial_{t} [(\partial_{t} n)^2 \nabla^2 n]$ and higher. These are absorbed into the projector and constraint-preserving flow, or treated as higher-order corrections suppressed near $\Pi_{*}$. Kinetic sign reversal risk exists if $1 + \chi \mathcal{E} < 0$; the theory requires $\chi > 0$ and $\mathcal{E} > 0$ bounds for stability.

Operator closure set:

$$\{k^2,\; k^4,\; k^2\omega^2\}$$

---

## 3. Solitons and Topology

Topological charge:

$$Q = \frac{1}{4\pi} \int d^3x\, \epsilon_{ijk}\, \partial_{i} n \cdot (\partial_{j} n \times \partial_{k} n)$$


| Structure | Topology |
| :--- | :--- |
| baryons | $Q=1$ |
| leptons | $Q=0$ kernel-bound modes |
| nuclei | $Q \ge 2$ composites |

Soliton mass:

$$m = \int d^3x\, \mathcal{E}_{\text{soliton}}$$

---

## 4. Kernel Response Sector

Nonlocal interactions:

$$\Phi(\mathbf{x}) = \int d^3x'\, K(\mathbf{x}-\mathbf{x}')\, n(\mathbf{x}')$$

Energy:

$$E_{\text{int}} = \frac12 \int d^3x\, n(\mathbf{x})\Phi(\mathbf{x})$$

Kernel structure:

$$K(k)=\frac{A}{k^2+\Lambda^2}+\frac{B}{k^2+m^2}$$

Real space:

$$K(r)=\frac{1}{4\pi \Delta r}(e^{-mr}-e^{-\Lambda r})$$

---

## 5. RG Flow Structure

Scale factor:

$$b=e^\ell$$

State vector:

$$X(\ell)=[K,\xi_{R},A,B,S_{i}]^T$$

### 5.1 Explicit Beta Functions (Derived)

**For $A_1$** (gradient stiffness):
$\beta_{A_1}=-A_1+cA_2\sigma_{\alpha}^2+\mathcal{O}(\chi\text{ mixing})$

**For $A_2$** (Skyrme):
$\beta_{A_2}=+2A_2+\gamma_{\sigma}'\sigma_{\alpha}^2A_2+\mathcal{O}(\text{higher})$

**For $A_{3}$** (biharmonic):
$\beta_{A_3}=-2A_3+\gamma_{\sigma}\sigma_{\alpha}^2+\mathcal{O}(\chi,A_2\text{ mixing})$

**For $\chi$** (refractive coupling):
$\beta_{\chi}=-\gamma_{\chi}\chi\sigma_{\alpha}^2+\eta_{\chi}\frac{\Pi^*}{A_1}\sigma_{\alpha}^2+\mathcal{O}(\chi^2\sigma_{\alpha}^4)$

**Kernel**:
$\frac{dK}{d\ell}=\beta_K$

**Response**: $`\frac{d\xi_R}{d\ell} = \xi_R \left[ \gamma_K \left( \frac{\partial \ln k}{\partial \ln K} \right)_{k \to 0} - \gamma_\sigma \sigma_\alpha^2 \right]`$



**Topological invariance**:
$\frac{dQ}{d\ell}=0$

### 5.2 RG-Locked Invariant & Closure

Complementary flows produce:

$$\Pi = g_{2} A_{3} \quad (\text{or } A_{2} A_{3})$$

with

$$\frac{d\Pi}{d\ell} \approx \mathcal{O}(\sigma_{\alpha}^2) \to \Pi_{*}$$

**v2.2 Note on Closure**: The set $\{k^2, k^4, k^2\omega^2\}$ is not strictly closed; loops can generate $\omega^4$ terms. Protection relies on the $S^2$ constraint and projector $P_{\perp}$ suppressing dangerous operators near the fixed point. Exact cancellation $`\gamma'_{\sigma} \approx -\gamma_{\sigma}`$ (or topological protection) is assumed for $`\Pi^{\ast}`$ stability; mismatch leads to drift.


---

## 6. Emergent Gravity-like Sector

Linearisation:

$$n = n_{\text{sol}(r)} + \psi$$

Equation:

$$Z_{t}(1+\chi E_{0})\partial_{t}^2\psi - A_{1}\nabla^2\psi = 0$$

Effective propagation:

$$c_{\text{eff}}(r)=\frac{c_{0}}{\sqrt{1+\chi E_{0}(r)}}$$

Optical metric (refractive):

$$n_{\text{opt}}(r)=\sqrt{1+\chi E_{0}(r)}$$

Observables:

$$\alpha(b)=\int \partial_{b} \delta n\,dz$$

$$\Delta t=\frac{1}{c_{0}}\int (n_{\text{opt}}-1)\,dz$$

**v2.2 Critique Response**: This is scalar refraction, not a full tensor metric. It reproduces leading lensing/time-delay effects but may deviate in precise massive-particle geodesics or factor-of-2 relations. Upgrade path: composite $g_{\mu\nu} \sim \eta_{\mu\nu} + \chi (\partial_{\mu} n \cdot \partial_{\nu} n)$ remains possible as emergent.

**RG Flow**:
$$\frac{d\ln f}{d\ell} \approx -\gamma_{\chi} \sigma_{\alpha}^2$$

---

## 7. Ensemble Gravity

Emergent potential (ensemble average over solitons via kernel):

$$\Phi_{\text{ens}}(r)\approx -V_{\text{flat}}^2 \ln(r/R_{0})$$

with

$$V_{\text{flat}}^2 \propto \chi \langle E_{0} \rangle \rho_{\rm sol} \Pi_{*}$$

Lensing:

$$\alpha \approx \pi V_{\text{flat}}^2$$

**RG Flow of Ensemble Potential**:
$$\beta_{V^2} \approx (-3 + \gamma_{\rm ens} \sigma_{\alpha}^2) V_{\rm flat}^2$$

Potential flattens in IR.

---

## 8. Surface-State Atomic Sector

Linearised operator:

$$\hat{\mathcal{L}}_{Q=1}\psi = P_{\perp}\left(A_{1}\nabla^2\psi - A_{3}\nabla^4\psi + V_{\text{eff}}(r)\psi\right)$$

Constraint:

$$n_{\text{sol}}\cdot \psi = 0$$

### 8.1 Kernel-Generated Effective Potential

$$\Delta V_{\text{eff}}(r)=\int d^3r'\,K(r-r')n_{\text{sol}}(r')$$

Result (soliton profile $n_{\text{sol}}(r)\sim e^{-r/R_{s}}$):

$$V_{\text{eff}}(r)= \frac{1}{4\pi \Delta} \left[ \frac{e^{-mr}}{r(1+m^2R_{s}^2)} - \frac{e^{-\Lambda r}}{r(1+\Lambda^2R_{s}^2)} \right]$$

### 8.2 Spectral Structure

Eigenvalues:

$$\lambda_{n,\ell} = -\frac{A_{1}}{\gamma^2(n+\ell+1)^2} -\frac{A_{3}}{\gamma^4(n+\ell+1)^4}$$

Curvature scale:

$$\gamma^{-2} \sim V_{\text{eff}}''(r_{0})$$

---

## 9. Stochastic Quantum Floor

Noise sector:

$$\hbar_{\text{eff}}=\sigma_{\alpha}^2\rho_{0}$$

Transport:

$$\beta' + \Gamma_{\beta} \beta = S_{\delta}\delta + \Xi_{\beta}$$

Noise:

$$\langle \Xi_{\beta} \Xi_{\beta} \rangle = N_{\beta} \delta(k-k')\delta(a-a')$$

**v2.2 Note**: Correlations $P_{\rm corr} = \cos^2(\theta_{A} - \theta_{\_B})$ require the noise kernel $\Xi$ to carry topological nonlocality inherited from the $S^2$ field structure, evading classical Bell bounds via field nonlocality rather than classical local realism.

---

## 10. Acoustic Cosmology

Pre-projection spectrum:

$$P(k)=\left[\frac{1-e^{-\Gamma(k)\tau_{\text{freeze}}}\cos(\omega(k)\tau_{\text{freeze}})}{H(k)}\right]^2$$

Damping:

$$\Gamma(k)=\left(\frac{\sigma_{\alpha}^2 A_{2}^2}{\Pi_{*}}\right)k^3$$

Hessian:

$$H(k)=A_{1}k^2+A_{3}k^4+A_{4}$$

Projection:

$$C_{\ell} \sim \int dk\,P(k)\,j_{\ell}^2(kD_{A})$$

---

## 11. Spectral Reduction & Diagnostic Layer (NON-FUNDAMENTAL)

This layer is explicitly **not part of the action or RG flow**.

### 11.1 Projection Functional

$$\mathcal{P}[n] \rightarrow \{P(k), C_{\ell}\}$$

### 11.2 Kernel of Observables


| Quantity | Status |
| :--- | :--- |
| SVD spectrum | projection basis diagnostic |
| peak structure | extremal mapping of $P(k)$ |
| transport flow | basis deformation measure |
| mode alignment | kernel eigenbasis rotation |

### 11.3 Key Principle
All numerical diagnostics are:
> eigenstructure of the projection operator, not modifications of the field theory.

---

## 12. Emergent Quantum Behaviour


| Phenomenon | Origin |
| :--- | :--- |
| interference | field overlap |
| tunnelling | boundary texture transition |
| decoherence | stochastic bath |
| spin | orientation texture |

---

## 13. Structural Nonlocality

Constraint:

$$|n|^2=1$$

Correlation:

$$P_{\text{corr}}=\cos^2(\theta_{A}-\theta_{B})$$

---

## 14. Minimal Interpretation

Spine reduces physics to:
* constrained continuum field
* nonlinear solitons
* kernel-mediated interactions
* RG flow structure (with explicit betas and $\Pi_{*}$ locking)
* stochastic bath dynamics
* spectral projection observables

No spacetime, particles, or operator Hilbert structure is fundamental.

---

**End of Document**
